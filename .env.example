COMPANY_NAME="Truvio Commerce"
SITE_NAME="Truvio Commerce Storefront"

# DynamicWeb 10 Delivery API backend (host root — the provider builds /dwapi/** paths).
DW_API_BASE="https://your-dw10-host"
DW_SHOP_ID="SHOP1"
DW_LANGUAGE_ID="ENU"
DW_CURRENCY_CODE="EUR"
DW_COUNTRY_CODE="DK"
DW_AREA_ID="5"
DW_REPOSITORY_NAME="Headless"
DW_QUERY_NAME="Products"

# Self-signed dev host ONLY — do not enable against production.
# DW_ALLOW_SELF_SIGNED="1"
# NODE_TLS_REJECT_UNAUTHORIZED="0"

# Optional shared secret guarding POST /api/revalidate.
DW_REVALIDATION_SECRET=""
